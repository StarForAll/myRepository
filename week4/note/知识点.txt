fopen,fwrite,fread是对open,write,read等系统函数的封装,是C语言库中函数,但在使用方面类似.
对文件描述符的操作,一个文件打开可以有多个文件描述符.

lseek(fd, 0, SEEK_SET):将文件指针放于文件的开头,将之后fwrite/write写入的内容放在开头.

read(fd, read_buf, sizeof(read_buf)):fd(文件描述符),read_buf(将文件内容写入字符数组中).
