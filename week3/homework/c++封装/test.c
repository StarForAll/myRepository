#include "AppleWrapper.h"  
#include <assert.h>  
#include<stdio.h>
  
int main(){  
    int  pApple = 11;  
    //int *handle = NULL;
    //pApple= 
    GetInstance(&pApple);  
    //SetColor(pApple, 1);  
    //int color = GetColor(pApple);  
    //printf("color = %d\n", color);  
    //ReleaseInstance(&pApple);  
    //assert(pApple == 0);  
    return 0;  
}
