在本目录中,实现的是对标准IO缓冲进行操作:
printf()->在不同的语境下使用的缓冲方式也不同.
          1.仅仅只是语句的输出显示(stdout),使用的是全缓冲.
          2.在语句中有换行符\n的出现,使用的是行缓存.
          3.使用fflush()进行缓冲区刷新输出/使用ffclose()关闭该文件流,立即执行对应的IO操作.
          4.使用setvbuf(stdout,NULL,_IONBF,0)说明缓冲方式为行缓冲.
